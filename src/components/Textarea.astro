---
const { label, name, id, rows = 4 } = Astro.props;
---

<div class="flow" style="--flow-spacer: 0.5rem">
  <label for={id} class="flex-group label">
    {label}
    <span class="visually-hidden">required</span>
    <span class="required" aria-hidden="true">*</span>
  </label>
  <textarea
    rows={rows}
    name={name}
    id={id}
    aria-invalid="false"
    aria-describedby="textArea-error"></textarea>
  <p class="error hidden" id="textArea-error"></p>
</div>

<style lang="scss">
  @use "../styles/abstracts/" as *;

  textarea {
    cursor: pointer;
    width: 100%;
    color: $form-input-color;
    font-family: $form-input-font-family;
    font-weight: $form-input-font-weight;
    font-size: $form-input-font-size;
    background-color: $form-input-background-color;
    border: 0;
    outline: $form-input-outline-default;
    padding: $form-input-padding;
    border-radius: $form-input-border-radius;
    resize: vertical;

    &:hover,
    &:focus {
      outline: $form-input-outline-hover-focus;
    }

    &[aria-invalid="true"] {
      outline-color: $form-input-error-color;
    }
  }
</style>
